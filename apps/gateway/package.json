{
  "name": "@apps/gateway",
  "scripts": {
    "build": "npm run codegen && tsc",
    "dev": "nodemon -r dotenv/config ./src/index.ts",
    "start": "node ./dist/index.js",
    "codegen": "APOLLO_ELV2_LICENSE=accept rover supergraph compose --config ./supergraph.yaml > supergraph.graphql"
  },
  "dependencies": {
    "@apollo/gateway": "^2.2.3",
    "@apollo/rover": "^0.11.0",
    "@apollo/server": "^4.3.1",
    "dataloader": "^2.1.0",
    "graphql": "^16.6.0",
    "lru-cache": "^7.14.1",
    "zod": "^3.20.2"
  },
  "devDependencies": {
    "dotenv": "^16.0.3"
  }
}
