overwrite: true
schema: 'http://localhost:5000/graphql'
generates:
  ./__generated/graphql/schema.ts:
    plugins:
      - 'typescript'
      - 'typescript-resolvers'
    config:
      federation: true
      mappers: {}
