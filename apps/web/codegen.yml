overwrite: true
schema: 'http://localhost:5000/graphql'
generates:
  ./app/_graphql/generated/schema.ts:
    plugins:
      - 'typescript'
      - 'typescript-resolvers'
    config:
      federation: true
      mappers: {}
