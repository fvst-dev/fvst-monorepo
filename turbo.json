{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build", "db:generate", "codegen"],
      "outputs": ["dist/**", ".next/**", "public/dist/**"],
      "env": [
        "VERCEL_URL",
        "PORT",
        "NODE_ENV",
        "DATABASE_URL",
        "CLERK_JWSK_URL",
        "CLERK_ISSUER"
      ]
    },
    "db:generate": {
      "cache": false
    },
    "codegen": {
      "cache": false
    },
    "lint": {
      "dependsOn": ["db:generate", "codegen"],
      "outputs": []
    },
    "format": {
      "outputs": []
    },
    "dev": {
      "cache": false
    },
    "clean": {
      "cache": false
    },
    "test": {
      "outputs": []
    },
    "precommit:format": {
      "cache": false
    }
  }
}
